comment = 'Production-ready PostgreSQL extension for rule engine with forward/backward chaining, rule repository, and event triggers'
default_version = '1.2.0'
module_pathname = '$libdir/rule_engine_postgre_extensions'
relocatable = false
requires = ''
trusted = false
