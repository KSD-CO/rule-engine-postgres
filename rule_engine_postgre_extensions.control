comment = 'Production-ready PostgreSQL extension for rule engine with forward/backward chaining, rule repository, event triggers, rule sets, and execution statistics'
default_version = '1.3.0'
module_pathname = '$libdir/rule_engine_postgre_extensions'
relocatable = false
requires = ''
trusted = false
