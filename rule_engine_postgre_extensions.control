comment = 'Production-ready PostgreSQL extension for rule engine with forward/backward chaining, rule repository, event triggers, rule sets, execution statistics, testing framework, validation, debugging, templates, and webhooks'
default_version = '1.5.0'
module_pathname = '$libdir/rule_engine_postgre_extensions'
relocatable = false
requires = ''
trusted = false
